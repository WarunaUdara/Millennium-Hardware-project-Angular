import { Routes } from '@angular/router';
import { HeroSectionComponent } from './common_components/hero-section/hero-section.component';

export const routes: Routes = [
    { path: 'heroes', component: HeroSectionComponent } 
];
